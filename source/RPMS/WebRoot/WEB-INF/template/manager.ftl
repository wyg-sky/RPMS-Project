package com.lion.${generateInfo.systemName}.${generateInfo.packageName}.manager;

import com.lion.${generateInfo.systemName}.${generateInfo.packageName}.model.${generateInfo.className};
import com.lion.system.common.manager.BusinessManager;

public interface ${generateInfo.className}Manager extends BusinessManager<${generateInfo.className}, String> {
	
}
